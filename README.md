* Maven_Jenkins_Parameters

The main intention of this project is to read the parameters from Jenkins during run time.

Based on the parameters passed by the user, the test will change its execution process.
